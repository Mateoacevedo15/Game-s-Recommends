import 'package:flutter/material.dart';

class RecoPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Página de Solicitud de Recomendación'),
      ),
      body: Center(
        child: Text('Contenido de la página de solicitud de recomendación'),
      ),
    );
  }
}
